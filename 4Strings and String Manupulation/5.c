// 5. Program to allow the user to enter details of an upcoming event (e.g., title, description, start time, and end time) and print the details in a format (including key-value pairs such as VERSION:2.0, CALSCALE:GREGORIAN, BEGIN:VEVENT, etc.) that can be imported into calendar applications (iCalendar/ics format). The code should include basic error handling for date formatting and should prompt the user to enter the correct format if invalid data is provided. 
